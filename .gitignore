node_modules/
.env
.env.local
*.log
.DS_Store
.etsy-tokens.json
.gdrive-tokens.json

# Firebase Admin service-account keys (never commit)
sa.json
*firebase-adminsdk*.json
*service*account*.json

# Python bytecode cache
__pycache__/
*.pyc
.grab-venv/

# Video clips pulled down by scripts/clip — big files, and this repo is public
clips/

# one-off mockup manifest (scripts/day-stickers-mockup.js)
.day-stickers.json

# Quoted audiobook text for the Cannon language film — public repo, so the
# lines themselves stay local; shots.json keeps citations and timings.
docs/cannon-language/lines.local.json
docs/cannon-language/*.log

# generated art output — the pictures are banked in Firebase Storage
# (raw, cutout and on-sky), so this folder is disposable
.artout/
