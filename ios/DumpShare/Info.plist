<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>NSExtension</key>
  <dict>
    <key>NSExtensionPointIdentifier</key>
    <string>com.apple.share-services</string>
    <key>NSExtensionPrincipalClass</key>
    <string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
    <key>NSExtensionAttributes</key>
    <dict>
      <!-- Photos, videos, and (Aug 2026) audio. There is no
           "SupportsAudioWithMaxCount" key, so audio rides on the generic FILE
           rule — that also surfaces "Send to Deck Factory" for other file
           types, where the sheet just says "Nothing to dump"; accepted
           trade-off over a fragile SUBQUERY predicate. Counts are generous
           because a crystal album or a folder of recordings is the normal
           case, not the exception. -->
      <key>NSExtensionActivationRule</key>
      <dict>
        <key>NSExtensionActivationSupportsImageWithMaxCount</key>
        <integer>500</integer>
        <key>NSExtensionActivationSupportsMovieWithMaxCount</key>
        <integer>100</integer>
        <key>NSExtensionActivationSupportsFileWithMaxCount</key>
        <integer>200</integer>
      </dict>
    </dict>
  </dict>
</dict>
</plist>
