# Generated by XcodeGen — regenerate with `xcodegen generate`
ImageForge.xcodeproj/

# Note: GoogleService-Info.plist IS committed here. The iOS API key it holds is
# not a sensitive secret (it ships in every published app binary and is
# restricted by bundle id + Firebase rules), so it's checked in for convenience.

# Xcode / build
build/
DerivedData/
*.xcuserstate
.DS_Store
