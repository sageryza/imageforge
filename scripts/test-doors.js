#!/usr/bin/env node
/* RETIRED with the doors game (2026-08-26, Sophie's note on the archive sweep
 * page: "get rid of this, including the page that it made"). The /doors route
 * is gone from server.js and public/doors.html is a tombstone, so there is
 * nothing left to test. Exits 0 so any script that still calls it stays green.
 * The old suite is in git history alongside the old page. */
console.log('test-doors: the doors game was retired 2026-08-26 — nothing to test.');
process.exit(0);
